name: civicweb_scraper
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10.0
  - pip
  - notebook
  - nbconvert
  - numpy
  - pandas
  - pip:
      - beautifulsoup4
      - joblib
      - requests
      - requests-cache
      - selenium
      - python-dotenv
      - tqdm
